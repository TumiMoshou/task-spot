import React from 'react';

const AddTaskBlock = () => {
    return (
        <div>
            <h3>Add Task</h3>
            <button>Add Task</button>
        </div>
    );
}

export default AddTaskBlock;
